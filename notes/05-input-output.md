# Input and Output in C

Input and output operations in C are performed using standard library functions.

## scanf()

The scanf() function is used to take input from the user.

Example:

int x;
scanf("%d", &x);

## printf()

The printf() function is used to display output.

Example:

printf("Hello World");